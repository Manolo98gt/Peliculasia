body {
    background-image: url('fondo.jpg'); /* Imagen de fondo */
    /* O, para video de fondo */
    background: none;
}

.video-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1; /* Para que el contenido esté sobre el video */
}
